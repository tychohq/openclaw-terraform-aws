# Terraform
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
!terraform.tfvars.advanced.example
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
tfplan

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# TLDR (coding agent state)
.tldr/
.tldrignore

# (PRDs live in meta/PRDs/)

# Secrets (never commit)
*.pem
*.key
secrets/
openclaw-secrets.*
openclaw-auth-profiles.json
.env
.env.*
!.env.example

# Content dirs — all content lives in mac-mini-setup, not here
openclaw-workspace/
openclaw-skills/
cron-jobs/
config/
